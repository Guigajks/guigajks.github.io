{
  "name": "cv-luiz-guilherme",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pdf": "npm run build && node scripts/generate-pdf.mjs"
  },
  "dependencies": {
    "astro": "^5.12.0",
    "yaml": "^2.8.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "playwright": "^1.54.0",
    "typescript": "^5.8.0"
  }
}
